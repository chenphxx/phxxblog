# 更新日志

## 2026.9.14

安全与性能

- 公开的文章详情接口不再返回作者的 IP 与归属地(此前仅作者本人可见的信息会随文章一起公开)
- 启动时校验密钥: 仍是仓库里的默认占位值或长度不足 32 则拒绝启动, 避免用公开常量签名令牌
- 上传改为扩展名白名单 + 图片文件头校验, 拒绝 SVG/HTML/JS 等可执行文档; 注册默认关闭
- 仅当请求来自可信反向代理时才采信 X-Forwarded-For(此前可伪造, 影响点赞去重、游客评论归属与 UV 统计)
- 登录增加失败限流(同 IP+账号 5 分钟内 10 次即 429)
- 管理员初始密码改为从环境变量读取或随机生成并打印, 不再硬编码
- 授权判断统一改为权限码, 不再判断角色名(角色 code 可被后台修改, 会静默失效)
- 关联加载策略按需指定: 每次鉴权请求不再全量拉取该用户的文章(实测后台页 SQL 从 15 条降到 7 条)
- 分类/标签的文章数改为一次 GROUP BY 统计(6 个分类从 7 条 SQL 降到 2 条)
- 媒体访问改为按上传根目录校验真实路径, 目录穿越与软链接逃逸一律拒绝(并修复了 41 条历史记录的失效路径)
- 新增数据保留策略: 访问明细默认保留 180 天、操作日志 365 天, 由清理脚本定期删除(趋势图与热力图读按日聚合数据, 不受影响)

工程与体验

- 新增后端测试(pytest + 内存 SQLite, 7 个用例)与 CI; 主题令牌、图标路径也纳入 CI 校验
- 启动时校验表结构与模型是否一致, 缺列则拒绝启动并打印 ALTER 语句; 新增 schema_version 表记录迁移
- 前端改为 Element Plus 按需引入: 首屏 JS 从 778 KB 降到 44.5 KB, 首屏 CSS 从 445 KB 降到 104.7 KB
- 修复搜索页在 keep-alive 缓存下切换分类/标签不刷新列表的问题
- 修复首页返回时不刷新文章列表的问题; 修复媒体管理多选框选中态不可靠的问题
- 请求失败统一打印带上下文的警告, 便于定位; 令牌过期时受保护页面统一跳转登录
- 令牌过期不再打断操作: 自动续期并重放原请求, 只有续期失败才回到登录页
- 补充可访问性(图标按钮的 aria-label、键盘可达的头像与链接切换)
- 写作页与后台编辑器的表单逻辑、文章与日记的导入导出流程各收敛为一份实现(去掉约 400 行重复代码)
- 前端单测从 9 个扩充到 38 个(编辑流程、导入导出分支、令牌续期), 新增首屏/整包体积度量脚本

文档

- 接口文档补全到全部 73 个接口(含鉴权要求与业务约束), 并提供从源码重新生成的脚本
- 数据库文档补上 diaries 与 schema_version 表, 新增「表结构演进」约定
- 架构文档更新前端实现章节(主题系统、字体、后台主题层、按需引入、令牌续期), 并补上数据保留与定时清理的部署说明
- `.env.example` 补齐全部可配置项(安全、令牌有效期、数据保留天数)并逐项注释

## 2026.9.13

- 日记模块增加导入导出功能 
- 增加切换主题的功能 

## 2026.9.12

- API文档仅管理员可访问并在管理后台新增入口 
- 代码现在会高亮展示
- 前端部分模块可在管理后台配置是否展示
- 一言现在每天仅自动更新一次, 可手动刷新
- 前端页脚内容支持自定义 
- 文章图片预览改为自适应窗口居中显示, 支持滚轮缩放 

## 2026.8.26

- 备案信息在管理后台填写添加, 默认不展示

## 2026.08.09

- 提供 start.bat 一键启动前后端
- 前后台支持文章的增删改查与发布/草稿
- 分类/标签支持输入即时新增, 封面支持本地上传或填写 URL, 新增公开/私密可见开关
- 文章管理支持勾选批量操作
- 管理后台新增文章导入导出功能
- 评论支持编辑/删除与多层回复
- 首页新增贡献热力图
- 新增对接UApiPro 一言、程序员历史上的今天、快递查询
- 管理后台-仪表盘数据卡片可点击跳转; 新增访问记录表格(IP/省市区/设备/时间); 访问趋势支持日/月/年统计、折线/柱状图与快捷时间段
- 用户管理支持重置密码与修改用户名; 个人资料支持修改用户名/昵称/邮箱
- 媒体库支持全选/反选、批量与单文件下载/删除; 操作日志增加省市区; 系统设置支持上传站点图标与编辑主页 README

## 2026.08.08

- 前后端分离: 前端 Vue3 + TypeScript + Vite(Element Plus / Vditor / Pinia), 后端 FastAPI + SQLAlchemy, 数据库 MySQL 9
- 完成账号管理: 注册/登录/JWT/刷新令牌/修改密码/修改邮箱, 用户/角色/权限
- 完成文章模块: 新增/编辑/删除/发布/草稿, 状态(草稿/审核中/已发布/私密/回收站), Markdown 编辑与渲染, 图片/视频/附件上传, 代码高亮与复制, 分类/标签, 归档时间轴, 阅读量/点赞
- 完成评论模块: 游客评论、回复、附件/链接预览, 记录 IP
- 完成统计模块: PV/UV/访问来源/浏览器/IP, 按日聚合, Dashboard 数据看板
- 完成操作日志、系统设置、全文搜索、RSS(`/rss.xml`)、Sitemap(`/sitemap.xml`)、深色/浅色主题

## 2024.10.9

- 添加了`notes`, `tools`, `write`三个网页以及`GitHub`个人主页的链接
- 添加了`viditor`项目, 用以在网站支持markdown编辑功能

## 2024.10.8

- 添加了第一个`main`网页
